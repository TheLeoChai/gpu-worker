# runtime secrets -- never commit
auth.env

# runtime state
logs/
*.log
jobs.json
jobs.json.tmp.*

# dependencies (npm install to restore)
node_modules/

# local backups
*.bak-*

# deploy runtime
deploy.log
deploy.log.old
deploy.lock

